What happens when you initialize a repository? Why do you need to do it?

	stuff

How is the staging area different from the working directory and the repository?
What value do you think it offers?

	The staging area allows for the user to see what changes are being made to the directory before commiting. Using status the user gets a preview.

How can you use the staging area to make sure you have one commit per logical change?
	
	Add only one file at a time, per change.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

	Branches are useful when a big change in the main code base is made. It allows to keep a functional main branch, while allowing the software to go into new directions.

How do the diagrams help you visualize the branch structure?

	I cheated

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
	
	Merging combines changes, the diagram shows the changes made in two branches interleaved. This helps find useful info about the code.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
	
	Automatic merging speeds up repetitive tasks, manual has a real brain thinks about what makes sense.
	
